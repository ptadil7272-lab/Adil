<meta name='viewport' content='width=device-width, initial-scale=1'/><!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Fresh Fruits Store</title>
<style>
    body {
        font-family: 'Poppins', sans-serif;
        background: #fafafa;
        margin: 0;
        padding: 0;
        color: #333;
    }

    header {
        background-color: #4CAF50;
        color: white;
        text-align: center;
        padding: 20px 0;
        font-size: 24px;
        font-weight: 600;
    }

    .product-container {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        padding: 20px;
    }

    .product {
        background: white;
        border-radius: 12px;
        box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        width: 260px;
        margin: 15px;
        text-align: center;
        padding: 15px;
        transition: transform 0.3s;
    }

    .product:hover {
        transform: translateY(-5px);
    }

    .product img {
        width: 100%;
        height: 180px;
        border-radius: 10px;
        object-fit: cover;
    }

    .product h3 {
        margin: 10px 0 5px;
    }

    .product p {
        color: #777;
        margin-bottom: 10px;
    }

    .buy-btn {
        background: #4CAF50;
        color: white;
        border: none;
        padding: 10px 15px;
        border-radius: 6px;
        cursor: pointer;
        font-weight: 500;
    }

    .buy-btn:hover {
        background: #45a049;
    }

    /* Customer Form Overlay */
    .overlay {
        position: fixed;
        top: 0; left: 0;
        width: 100%; height: 100%;
        background: rgba(0,0,0,0.6);
        display: none;
        align-items: center;
        justify-content: center;
    }

    .form-box {
        background: white;
        padding: 25px;
        border-radius: 10px;
        width: 320px;
        box-shadow: 0 4px 10px rgba(0,0,0,0.2);
        text-align: left;
    }

    .form-box h2 {
        text-align: center;
        margin-bottom: 15px;
    }

    .form-box input, .form-box textarea {
        width: 100%;
        padding: 8px;
        margin: 6px 0 12px;
        border: 1px solid #ccc;
        border-radius: 5px;
    }

    .form-box button {
        background: #4CAF50;
        color: white;
        border: none;
        padding: 10px 15px;
        width: 100%;
        border-radius: 6px;
        cursor: pointer;
    }

    .close-btn {
        background: #f44336;
        margin-top: 8px;
    }
</style>
</head>
<body>

<header>🍎 Fresh Fruits Store 🍌</header>

<div class="product-container">
    <div class="product">
        <img src="https://upload.wikimedia.org/wikipedia/commons/1/15/Red_Apple.jpg" alt="Apple">
        <h3>Apple</h3>
        <p>₹120 / Kg</p>
        <button class="buy-btn" onclick="openForm('Apple',120)">Buy Now</button>
    </div>

    <div class="product">
        <img src="https://upload.wikimedia.org/wikipedia/commons/8/8a/Banana-Single.jpg" alt="Banana">
        <h3>Banana</h3>
        <p>₹60 / Dozen</p>
        <button class="buy-btn" onclick="openForm('Banana',60)">Buy Now</button>
    </div>

    <div class="product">
        <img src="https://upload.wikimedia.org/wikipedia/commons/1/1c/Orange-Fruit-Pieces.jpg" alt="Orange">
        <h3>Orange</h3>
        <p>₹90 / Kg</p>
        <button class="buy-btn" onclick="openForm('Orange',90)">Buy Now</button>
    </div>

    <div class="product">
        <img src="https://upload.wikimedia.org/wikipedia/commons/2/2f/Mango.jpg" alt="Mango">
        <h3>Mango</h3>
        <p>₹150 / Kg</p>
        <button class="buy-btn" onclick="openForm('Mango',150)">Buy Now</button>
    </div>
</div>

<!-- Customer Form -->
<div class="overlay" id="formOverlay">
    <div class="form-box">
        <h2>Order Details</h2>
        <form id="orderForm">
            <label>Product</label>
            <input type="text" id="product" readonly>

            <label>Name</label>
            <input type="text" id="name" placeholder="Enter your name" required>

            <label>Address</label>
            <textarea id="address" placeholder="Enter your address" required></textarea>

            <label>Quantity</label>
            <input type="number" id="quantity" placeholder="Enter quantity" min="1" required>

            <button type="submit">Submit Order</button>
            <button type="button" class="close-btn" onclick="closeForm()">Cancel</button>
        </form>
    </div>
</div>

<script>
    let selectedProduct = '';
    let selectedPrice = 0;

    function openForm(product, price) {
        selectedProduct = product;
        selectedPrice = price;
        document.getElementById("formOverlay").style.display = "flex";
        document.getElementById("product").value = product + " - ₹" + price;
    }

    function closeForm() {
        document.getElementById("formOverlay").style.display = "none";
        document.getElementById("orderForm").reset();
    }

    document.getElementById("orderForm").addEventListener("submit", function(e) {
        e.preventDefault();

        const name = document.getElementById("name").value.trim();
        const address = document.getElementById("address").value.trim();
        const quantity = document.getElementById("quantity").value.trim();

        const message = `🛍 New Fruit Order
-----------------------------
🍉 Product: ${selectedProduct}
💰 Price: ₹${selectedPrice}
📦 Quantity: ${quantity}
👤 Name: ${name}
🏠 Address: ${address}`;

        const phoneNumber = "8921096589";
        const whatsappURL = `https://wa.me/${phoneNumber}?text=${encodeURIComponent(message)}`;

        // Send data to WhatsApp (without redirecting user)
        fetch(whatsappURL, {mode: 'no-cors'});

        alert("✅ Your order has been placed successfully!");
        closeForm();
    });
</script>

</body>
</html>
